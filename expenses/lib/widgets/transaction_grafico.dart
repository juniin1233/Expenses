import 'package:flutter/material.dart';

class TransactionGrafico extends StatelessWidget {

  const TransactionGrafico({ super.key });

   @override
   Widget build(BuildContext context) {
       return Container(
            width: double.infinity,
            child: Card(
              child: Text("Gráfico"),
            ),
          );
  }
}